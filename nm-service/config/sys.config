[
 {kernel,
  [{logger,
    [{handler, default, logger_std_h,
      #{level => info,
        formatter => {logger_formatter, #{legacy_header => false,
                                          single_line => true,
                                          time_offset => "Z"}}}}]},
   {logger_level, info}
  ]},

 {{{name}},
  []}
].
